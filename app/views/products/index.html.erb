<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product List</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
      background-image: url('https://media.istockphoto.com/id/1146409825/vi/vec-to/minh-h%E1%BB%8Da-c%C3%A1-c%C3%A1-c%C3%A1-c%E1%BA%A3nh-%C4%91%E1%BB%8F.jpg?s=612x612&w=0&k=20&c=_LRHfzYggSj5kce-Zdjo7k91nvy-07u9V1tNg7yDJOo=;');
      background-size: contain;
      background-position: center;
      background-repeat: no-repeat;
      background-attachment: fixed;
    }

    /* Style cho menu */
    .navbar {
      background-color: #333;
      overflow: hidden;
    }

    .navbar a {
      float: left;
      display: block;
      color: white;
      text-align: center;
      padding: 14px 20px;
      text-decoration: none;
    }

    .navbar a:hover {
      background-color: #ddd;
      color: black;
    }

    h1 {
      text-align: center;
      margin-top: 20px;
      font-size: 36px; /* Font size for heading */
      font-weight: bold; /* Make the font bold */
      font-style: italic; /* Make the font italic */
      color: black; /* Text color */
      background-color: white; /* Background color */
      display: flex; /* Flexbox layout for icon and text */
      align-items: center; /* Vertically align icon and text */
      justify-content: center; /* Center the content */
      border-radius: 12px; /* Rounded corners for the background */
      padding: 10px 20px; /* Padding around the content */
      border: 1px solid #ddd; /* Light border around the heading */
    }

    h1 img {
      width: 60px;
      height: 60px;
      margin-right: 15px; /* Space between the icon and text */
    }

    #product-list {
      width: 100%;
      margin: 30px auto;
      display: flex;
      flex-wrap: wrap;
      justify-content: space-between;
      padding: 0 20px;
    }

    .product-card {
      background-color: #fff;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      width: 24%; /* Adjusted to fit 4 products per row */
      height: auto;
      margin-bottom: 20px;
      padding: 10px;
      text-align: center;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      border: 1px solid #ddd;
      transition: transform 0.3s ease;
    }

    .product-card:hover {
      transform: scale(1.05);
    }

    .product-image img {
      width: 100%;
      height: 100px;
      object-fit: cover;
      margin-bottom: 10px;
    }

    .product-name {
  font-size: 16px;
  font-weight: bold;
  margin-bottom: 5px;
  color: black; /* Default text color */
  text-decoration: none; /* Remove underline */
  transition: color 0.3s ease; /* Smooth transition for color change */
}

.product-name:hover {
  color: #007bff; /* Blue color on hover */
  text-decoration: none; /* Ensure there's no underline on hover */
}


    .product-description {
      font-size: 12px;
      color: #555;
      margin-bottom: 10px;
      text-overflow: ellipsis;
      overflow: hidden;
      display: -webkit-box;
      -webkit-line-clamp: 2;
      -webkit-box-orient: vertical;
    }

    .product-stock {
      font-size: 12px;
      color: #333;
      margin-bottom: 10px;
    }

    .actions {
      display: flex;
      justify-content: space-between;
      background-color: #e0e0e0;
      padding: 5px;
    }

    .btn-action {
      text-decoration: none;
      color: black;
      padding: 5px;
      background-color: transparent;
      border: none;
      cursor: pointer;
      transition: color 0.3s ease;
    }

    .btn-action:hover {
      color: red;
    }

    .see-more {
      font-size: 12px;
      color: #007bff;
    }

    .see-more:hover {
      text-decoration: underline;
    }

    /* Carousel container styling */
    .carousel {
      position: relative;
      width: 80%; /* Make the carousel smaller, e.g., 80% of the screen width */
      height: 300px; /* Adjust the height of the carousel */
      margin: 20px auto; /* Center the carousel with auto margins */
      border: 2px solid #333; /* Add a border around the carousel */
      border-radius: 10px; /* Add rounded corners */
      overflow: hidden; /* Ensure images don't overflow the carousel container */
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Add a subtle shadow */
      background-color: #fff; /* Add a background color */
    }

    /* Styling for carousel images */
    .carousel img {
      width: 100%;
      height: 100%;
      object-fit: cover; /* Ensure images cover the container without stretching */
      position: absolute;
      left: 0;
      top: 0;
      transition: opacity 1s ease-in-out; /* Smooth image transition */
      opacity: 0; /* Start images with 0 opacity */
    }

    .carousel img.active {
      opacity: 1; /* Show active image */
    }

    /* Navigation buttons */
    .carousel-button {
      position: absolute;
      top: 50%;
      transform: translateY(-50%);
      background-color: rgba(0, 0, 0, 0.5);
      color: white;
      font-size: 20px;
      padding: 10px;
      border: none;
      cursor: pointer;
      z-index: 1;
    }

    .carousel-button.left {
      left: 10px;
    }

    .carousel-button.right {
      right: 10px;
    }
  </style>
</head>
<body>

  <h1>
  
  Danh sách sản phẩm đồ câu
</h1>


  <!-- Carousel -->
  <div class="carousel">
    <img src="https://wwin.vn/wp-content/uploads/2023/06/cach-chon-can-cau-ca-1.png;" class="carousel-image active">
    <img src="https://i.ytimg.com/vi/etoFcZ1vlJY/hq720.jpg?sqp=-oaymwEhCK4FEIIDSFryq4qpAxMIARUAAAAAGAElAADIQj0AgKJD&rs=AOn4CLD59Vx6tGjahlfo0ThGXKqDDiCUgA;" class="carousel-image">
    <img src="https://i.ytimg.com/vi/BzhOLZhqJio/hq720.jpg?sqp=-oaymwEhCK4FEIIDSFryq4qpAxMIARUAAAAAGAElAADIQj0AgKJD&rs=AOn4CLDu3R2usTu_XhLcb5z2mBoTD0SeFg;" class="carousel-image">

    <!-- Navigation buttons -->
    <button class="carousel-button left" onclick="prevImage()">❮</button>
    <button class="carousel-button right" onclick="nextImage()">❯</button>
  </div>

  <div id="product-list">
    <% @products.each do |product| %>
      <div class="product-card">
        <div class="product-image">
          <% if product.featured_image.attached? %>
            <%= image_tag url_for(product.featured_image), alt: "Product image", class: "product-image" %>
          <% end %>
        </div>

        <h2 class="product-name"><%= link_to product.name, product %></h2>

        <p class="product-description">
          <%= truncate(sanitize(product.description.to_s, tags: %w(p b i u br a)), length: 50) %>
          <% if product.description.to_s.length > 50 %>
            <%= link_to "Xem thêm", product, class: "see-more" %>
          <% end %>
        </p>

        <p class="product-stock">Quantity: <%= product.inventory_count %></p>

        <div class="actions">
          <%= link_to 'Edit', edit_product_path(product), class: "btn-action" %>
          <%= button_to 'Delete', product_path(product), method: :delete, class: "btn-action" %>
        </div>
      </div>
    <% end %>
  </div>

  <script>
    let currentIndex = 0;
    const images = document.querySelectorAll('.carousel-image');

    function showImage(index) {
      images.forEach((image, i) => {
        image.classList.remove('active');
        if (i === index) {
          image.classList.add('active');
        }
      });
    }

    function nextImage() {
      currentIndex = (currentIndex + 1) % images.length;
      showImage(currentIndex);
    }

    function prevImage() {
      currentIndex = (currentIndex - 1 + images.length) % images.length;
      showImage(currentIndex);
    }

    setInterval(nextImage, 5000);
  </script>

</body>
</html>
